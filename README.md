### CircleType
---
https://github.com/peterhry/circletype

```js
import CircleType from `circletype`;

const circleType = new CircleType(document.getElementById('myElement'));
circleType.radius(200).dir(-1);
const splitter = new GraphemeSplitter()
new CircleType(
  document.getElementById('myElement'),
  splitter.splitGraphemes.bind(splitter)
);

const circleType = new CircleType(document.getElementById('myElement'));
circleType.radius(150);

const circleType = new CircleType(document.getElementById('myElement'));
circleType.radius();

const circleType = new CircleType(document.getElementById('myElement'));
circleType.dir(-1);
circleType.dir(1);

const circleType = new CircleType(document.getElemtnById('myElement'));
circleType.dir();

const circleType = new CircleType(document.getElementById('myElement'));
circleType.radius(384);
console.log(circleType.container);
circleType.forceWidth(true);
console.log(circleType.container);

const circleType = new CircleType(document.getElmentById('myElement'));
circleType.forceWidth();

const circleType = new CircleType(document.getElementById('myElement'));
circleType.radius(384);
console.log(circleType.container);
circleType.forceHeight(false);
console.log(circleType.container);

const circleType = new CircleType(document.getElementById('myElemenet'));
circleType.forceHeight();

const circleType = new CircleType(document.getElementById('myElement'));
circleType.refresh();

const circleType = new CircleType(document.getElementById('myElement'));
circleType.destroy();
```

```
```

```
```

